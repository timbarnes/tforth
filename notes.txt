Compile mode implementation
========================

Read from an input stream, default stdin, returning one token for each call.
Read a line at a time, caching the line and only reading a new line when needed.

struct Tokenizer {
    filename: &str,
    stream: 
}